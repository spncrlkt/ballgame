# Focused Analysis Report

DB: `db/tournament_20260127_153935.db`

## SQL Used
```
SELECT COUNT(*) FROM matches
SELECT AVG(duration_secs) FROM matches
SELECT AVG(score_left + score_right) FROM matches
SELECT AVG(CASE WHEN score_left + score_right = 0 THEN 1.0 ELSE 0.0 END) FROM matches
SELECT SUM(shots_attempted), SUM(shots_made) FROM player_stats
SELECT AVG(avg_shot_quality) FROM player_stats
SELECT match_id, time_ms, data FROM events WHERE event_type = 'SS'
SELECT match_id, time_ms, data FROM events WHERE event_type = 'SR'
SELECT id, level_name FROM matches
SELECT match_id, COUNT(*) FROM events WHERE event_type = 'AG' GROUP BY match_id
SELECT match_id, COUNT(*) FROM events WHERE event_type = 'SA' GROUP BY match_id
SELECT event_type, COUNT(*) FROM events GROUP BY event_type

    SELECT level_name, COUNT(*)
    FROM (
        SELECT m.id, m.level_name, SUM(ps.shots_attempted) as shots
        FROM matches m JOIN player_stats ps ON ps.match_id = m.id
        GROUP BY m.id
    )
    WHERE shots <= 5
    GROUP BY level_name
    ORDER BY COUNT(*) DESC


    SELECT
        m.level_name,
        COUNT(DISTINCT m.id) as matches,
        AVG(m.score_left + m.score_right) as goals_per_match,
        SUM(ps.shots_attempted) as shots,
        SUM(ps.shots_made) as made,
        AVG(CASE WHEN m.score_left + m.score_right = 0 THEN 1.0 ELSE 0.0 END) as scoreless_rate,
        AVG(ps.avg_shot_quality) as avg_shot_quality
    FROM matches m
    JOIN player_stats ps ON ps.match_id = m.id
    GROUP BY m.level_name
    ORDER BY scoreless_rate DESC, shots ASC

```

## Summary
- Matches: 24
- Avg duration: 51.13s
- Goals/match: 2.167
- Shots/match: 9.833
- Shot%: 0.203
- Avg shot quality: 0.418
- Scoreless rate: 0.125
- Steal success rate: 0.261

## Shot Quality Distribution (from SS events)
- avg 0.401 | med 0.410 | p10 Some(0.27) | p90 Some(0.5)

## Shot Charge (from SR events)
- avg 0.333 | med 0.290 | p10 Some(0.17) | p90 Some(0.58)

## Charge Durations (SS -> SR)
- avg 2.870s | med 0.500s | p10 Some(0.267) | p90 Some(9.367)

## Distance vs Charge (SS position -> target basket)
- correlation: 0.130
distance | shots | avg_charge
---- | ---- | ----
0-200 | 3 | 0.383
200-400 | 9 | 0.322
400-600 | 46 | 0.326
600-800 | 132 | 0.312
800+ | 46 | 0.395

## AI Goal Changes / Match
- avg 64.46 | med 65.00 | p10 Some(36.0) | p90 Some(86.0)

## Steal Attempts / Match
- avg 23.46 | med 21.00 | p10 Some(5.0) | p90 Some(48.0)

## Low-shot Levels (<= 5 shots/match)
- Pursuit Arena 2: 2

## Per-level Summary
level | matches | goals/m | shots/m | shot% | scoreless | avg_q
---- | ---- | ---- | ---- | ---- | ---- | ----
Skyway | 5 | 1.00 | 10.80 | 0.074 | 0.400 | 0.417
Pursuit Arena | 5 | 2.40 | 9.80 | 0.224 | 0.200 | 0.422
Slopes | 4 | 1.75 | 9.50 | 0.158 | 0.000 | 0.404
Pursuit Arena 2 | 5 | 2.60 | 7.80 | 0.333 | 0.000 | 0.388
Open Floor | 5 | 3.00 | 11.20 | 0.250 | 0.000 | 0.454
